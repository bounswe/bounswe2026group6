{
  "name": "neph-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Minimal backend scaffold for Neighborhood Emergency Preparedness Hub",
  "main": "src/server.js",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "test": "jest --runInBand",
    "test:unit": "jest --selectProjects unit",
    "test:integration": "jest --selectProjects integration --runInBand",
    "test:watch": "jest --watch"
  },
  "devDependencies": {
    "jest": "^30.0.5",
    "supertest": "^7.1.1"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "express-rate-limit": "^8.3.1",
    "jsonwebtoken": "^9.0.3",
    "nodemailer": "^8.0.4",
    "pg": "^8.13.3",
    "uuid": "^13.0.0"
  }
}
